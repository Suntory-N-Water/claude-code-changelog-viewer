rules:
  artipacked:
    ignore:
      # fetch-docs.yml needs persist-credentials to push commits
      - fetch-docs.yml:38
      # fetch-changelog.yml needs persist-credentials to push commits
      - fetch-changelog.yml:27
