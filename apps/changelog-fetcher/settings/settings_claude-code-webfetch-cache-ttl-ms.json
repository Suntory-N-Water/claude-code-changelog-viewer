{
  "key": "CLAUDE_CODE_WEBFETCH_CACHE_TTL_MS",
  "leaf_name": "CLAUDE_CODE_WEBFETCH_CACHE_TTL_MS",
  "slug": "claude-code-webfetch-cache-ttl-ms",
  "source": "env",
  "description_en": "Set to the number of milliseconds WebFetch keeps each fetched URL's response cached. The default is 900000, which is 15 minutes. Takes plain digits only; 0, a decimal, or any other spelling keeps the default. Claude Code reads the value once per launch, so a change in a settings env block applies when you next launch claude. Requires Claude Code v2.1.233 or later",
  "description_ja": "WebFetchが取得したURLのレスポンスをキャッシュに保持する時間をミリ秒単位で設定します（デフォルトは15分を表す900000、Claude Code v2.1.233以降が必要）。",
  "use_case_ja": "- WebFetchによる同一URLへの繰り返しアクセスを高速化するキャッシュの保持期間（デフォルト：15分）を、開発環境やネットワーク状況に合わせて変更したい場合に使用します\n- 設定値は半角数字のみを受け付け、0や小数点、その他の文字を指定した場合はデフォルトの15分（900000ミリ秒）が維持されます\n- 設定値は Claude 起動時に一度だけ読み込まれるため、設定の環境変数ブロックを変更した場合は次回 claude を起動した際から有効になります",
  "parent_descriptions": [],
  "doc_snippets": [
    "HTTP URLs are automatically upgraded to HTTPS. Large pages are truncated to a fixed character limit before processing. WebFetch caches each response for 15 minutes by default, so repeated fetches of the same URL quickly. On Claude Code v2.1.233 or later, set CLAUDE_CODE_WEBFETCH_CACHE_TTL_MS to change how long WebFetch keeps each response. When a URL redirects to a different host, WebFetch s a text result that names the original URL and the redirect target instead of following it. Claude then fetches the new URL with a second WebFetch call. When the extraction step hits an overloaded API, Claude Code retries it with backoff; a fetch that still fails s an error result. Before v2.1.212, the API error text could reach Claude as if it were the extracted page content."
  ],
  "fetched_at": "2026-08-17",
  "related_changelog": []
}
