{
  "key": "CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR",
  "leaf_name": "CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR",
  "slug": "claude-bash-maintain-project-working-dir",
  "source": "env",
  "description_en": "Return to the original working directory after each Bash or PowerShell command in the main session",
  "description_ja": "メインセッションにおいて、BashやPowerShellコマンドの実行後に常に元の作業ディレクトリに戻るようにします。",
  "use_case_ja": "- デフォルトのディレクトリ移動(cd)の引き継ぎを無効化し、すべてのコマンドを常にプロジェクトルートなどの初期ディレクトリから開始させたい場合に使用します。\n- 意図しないディレクトリでのコマンド実行を防ぎ、シェル操作の冪等性を高めるのに役立ちます。",
  "parent_descriptions": [],
  "doc_snippets": [
    "When Claude runs cd in the main session, the new working directory carries over to later Bash commands as long as it stays inside the project directory or an additional working directory you added with --add-dir , /add-dir , or additionalDirectories in settings. Subagent sessions never carry over working directory changes. If cd lands outside those directories, Claude Code resets to the project directory and appends Shell cwd was reset to to the tool result. To disable this carry-over so every Bash command starts in the project directory, set CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR=1 . Environment variables do not persist. An export in one command will not be available in the next. Activate your virtualenv or conda environment before launching Claude Code. To make environment variables persist across Bash commands, set CLAUDE_ENV_FILE to a shell script before launching Claude Code, or use a SessionStart hook to populate it dynamically.",
    "\"shell\": \"powershell\" on individual command hooks : runs that hook in PowerShell. Hooks spawn PowerShell directly, so this works regardless of CLAUDE_CODE_USE_POWERSHELL_TOOL . shell: powershell in skill frontmatter : runs !`command` blocks in PowerShell. Requires the PowerShell tool to be enabled. The same main-session working-directory reset behavior described under the Bash tool section applies to PowerShell commands, including the CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR environment variable. Preview limitations"
  ],
  "official_doc_urls": ["https://code.claude.com/docs/en/tools-reference"],
  "related_changelog": [
    {
      "version": "1.0.18",
      "content": "- Added CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR environment variable to freeze working directory for bash commands"
    }
  ],
  "fetched_at": "2000-01-01"
}
