{
  "key": "MCP_TIMEOUT",
  "slug": "mcp-timeout",
  "source": "env",
  "description_en": "Timeout in milliseconds for MCP server startup (default: 30000, or 30 seconds)",
  "description_ja": "MCPサーバー起動時のタイムアウト(ミリ秒単位)を設定します(デフォルト：30000、30秒)。",
  "related_changelog": [
    {
      "version": "0.2.41",
      "content": "- MCP server startup timeout can now be configured via MCP_TIMEOUT environment variable"
    },
    {
      "version": "1.0.8",
      "content": "- Fixed edge cases where MCP_TIMEOUT and MCP_TOOL_TIMEOUT weren't being respected"
    }
  ]
}
