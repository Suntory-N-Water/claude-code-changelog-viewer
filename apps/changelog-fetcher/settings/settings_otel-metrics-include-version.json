{
  "key": "OTEL_METRICS_INCLUDE_VERSION",
  "leaf_name": "OTEL_METRICS_INCLUDE_VERSION",
  "slug": "otel-metrics-include-version",
  "source": "env",
  "description_en": "Set to `true` to include Claude Code version in metrics attributes (default: excluded). See [Monitoring](/en/monitoring-usage)",
  "description_ja": "trueに設定すると、メトリクスの属性にClaude Codeのバージョンを含めます(デフォルトは除外されます)。",
  "use_case_ja": "- 特定のバージョンにおけるパフォーマンスやエラー率を分析し、アップデートの影響を評価するために使用します。",
  "parent_descriptions": [],
  "doc_snippets": [
    "Environment Variable Description Default Value Example to Disable OTEL_METRICS_INCLUDE_SESSION_ID Include session.id attribute in metrics true false OTEL_METRICS_INCLUDE_VERSION Include app.version attribute in metrics OTEL_METRICS_INCLUDE_ACCOUNT_UUID Include user.account_uuid and user.account_id attributes in metrics These variables help control the cardinality of metrics, which affects storage requirements and query performance in your metrics backend. Lower cardinality generally means better performance and lower storage costs but less granular data for analysis.",
    "Attribute Description Controlled By session.id Unique session identifier OTEL_METRICS_INCLUDE_SESSION_ID (default: true) app.version Current Claude Code version OTEL_METRICS_INCLUDE_VERSION (default: false) organization.id Organization UUID (when authenticated) Always included when available user.account_uuid Account UUID (when authenticated) OTEL_METRICS_INCLUDE_ACCOUNT_UUID (default: true) user.account_id Account ID in tagged format matching Anthropic admin APIs (when authenticated), such as user_01BWBeN28..."
  ],
  "official_doc_urls": ["https://code.claude.com/docs/en/monitoring-usage"],
  "related_changelog": [],
  "fetched_at": "2000-01-01"
}
