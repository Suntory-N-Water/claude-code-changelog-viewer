{
  "key": "CLAUDE_CODE_USE_ANTHROPIC_AWS",
  "leaf_name": "CLAUDE_CODE_USE_ANTHROPIC_AWS",
  "slug": "claude-code-use-anthropic-aws",
  "source": "env",
  "description_en": "Use [Claude Platform on AWS](/en/claude-platform-on-aws)",
  "description_ja": "AWS上のClaudeプラットフォーム（Claude Platform on AWS）を使用するように設定します。",
  "use_case_ja": "- デフォルトのAnthropic APIではなく、AWS上でホストされているClaudeのインフラストラクチャをバックエンドとして利用したい場合に有効化します。\n- Agent SDKなどを介して、プログラムからAWS上のClaudeをターゲットにする際の起点となるスイッチとして機能します。",
  "parent_descriptions": [],
  "doc_snippets": [
    "Set the environment variables that route Claude Code through Claude Platform on AWS instead of the default Anthropic API.",
    "Use the Agent SDK The Agent SDK reads the same environment variables as the CLI, so any program that spawns the Claude Code subprocess can target Claude Platform on AWS by exporting CLAUDE_CODE_USE_ANTHROPIC_AWS , ANTHROPIC_AWS_WORKSPACE_ID , and either ANTHROPIC_AWS_API_KEY or AWS credentials before the call.",
    "To route traffic through a proxy or LLM gateway , set ANTHROPIC_AWS_BASE_URL to the proxy's address. Claude Code sends requests to that URL with the same workspace and authentication headers, so any gateway that forwards them unchanged works.",
    "If your gateway signs requests itself, set CLAUDE_CODE_SKIP_ANTHROPIC_AWS_AUTH=1 so Claude Code sends unsigned requests and lets the gateway add SigV4 headers before forwarding to AWS. If the gateway requires its own token, set it in ANTHROPIC_AUTH_TOKEN .",
    "Requests still go to api.anthropic.com CLAUDE_CODE_USE_ANTHROPIC_AWS is likely unset or set to a value that doesn't parse as truthy. Set it to 1 and run /status to confirm the resolved provider. If CLAUDE_CODE_USE_BEDROCK or CLAUDE_CODE_USE_FOUNDRY is also set, those take precedence over Claude Platform on AWS. Additional resources",
    "Service Claude API Vertex API Bedrock API Foundry API Claude Platform on AWS Anthropic (Metrics) Default on. DISABLE_TELEMETRY=1 to disable. Default off. CLAUDE_CODE_USE_VERTEX must be 1. Default off. CLAUDE_CODE_USE_BEDROCK must be 1. Default off. CLAUDE_CODE_USE_FOUNDRY must be 1. Default off. CLAUDE_CODE_USE_ANTHROPIC_AWS must be 1. Sentry (Errors) Default on. DISABLE_ERROR_REPORTING=1 to disable. Claude API ( /feedback reports) Default on. DISABLE_FEEDBACK_COMMAND=1 to disable. Session quality surveys Default on. CLAUDE_CODE_DISABLE_FEEDBACK_SURVEY=1 to disable. WebFetch domain safety check Default on. skipWebFetchPreflight: true in settings to disable."
  ],
  "related_changelog": []
}
