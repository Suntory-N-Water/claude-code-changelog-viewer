{
  "key": "CLAUDE_CODE_MCP_ALLOWLIST_ENV",
  "slug": "claude-code-mcp-allowlist-env",
  "source": "env",
  "description_en": "Set to `1` to spawn stdio MCP servers with only a safe baseline environment plus the server's configured `env`, instead of inheriting your shell environment",
  "description_ja": "1に設定すると、シェル環境を継承せず、最小限の安全な環境変数と設定されたenvのみでstdio MCPサーバーを起動します。",
  "related_changelog": []
}
