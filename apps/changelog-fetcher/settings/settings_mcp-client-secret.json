{
  "key": "MCP_CLIENT_SECRET",
  "slug": "mcp-client-secret",
  "source": "env",
  "description_en": "OAuth client secret for MCP servers that require [pre-configured credentials](/en/mcp#use-pre-configured-oauth-credentials). Avoids the interactive prompt when adding a server with `--client-secret`",
  "description_ja": "事前設定済みの認証情報が必要なMCPサーバー用のOAuthクライアントシークレットを指定し、対話的な入力を回避します。",
  "related_changelog": []
}
