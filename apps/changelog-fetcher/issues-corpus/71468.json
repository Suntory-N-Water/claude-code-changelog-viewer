{
  "number": 71468,
  "title": "[Bug] Accidental mouse clicks on selection prompts misfire; mitigation breaks fullscreen scrollback",
  "body": "**Bug Description**\n# Accidental mouse clicks on selection/permission prompts easily misfire; the only mitigation env var is undiscoverable, and combining it with fullscreen breaks scrollback\r\r## Summary\rTwo related problems with mouse handling in the fullscreen renderer:\r\r1. Interactive selection prompts (tool-permission prompts, AskUserQuestion\r   option menus, `/model`, `/config`) became mouse-clickable. A single stray\r   click confirms/rejects an option with no deliberate confirm step, so\r   accidental clicks easily misfire — including rejecting a tool-permission\r   prompt.\r2. The only mitigation, the env var `CLAUDE_CODE_DISABLE_MOUSE=1`, is hard to\r   discover (no `settings.json` key, not surfaced in `/config` or docs), and\r   when combined with the fullscreen renderer it makes scrollback look broken\r   — the conversation appears truncated/\"lost\" even though the data is intact.\r\r## Environment\r- Claude Code v2.1.193 (env var presence confirmed in the installed binary)\r- macOS (Darwin 25.5.0), fish shell\r- Fullscreen renderer (mouse capture enabled, introduced ~v2.1.187)\r\r## Problem 1 — accidental click misfire\r\r### Repro / observed behavior\r1. A selection prompt is on screen (e.g. a tool-permission prompt).\r2. A stray/accidental mouse click lands on the prompt area.\r3. The click immediately selects/confirms (or rejects) an option — there is\r   no second deliberate confirm step. In a single session this caused a\r   tool-use permission prompt to be rejected twice by accidental clicks.\r\r### Why it's a problem\rUsers naturally move and click the mouse for unrelated reasons (scrolling,\rwindow focus, switching apps) while a prompt is visible. A single stray click\rthat confirms a choice is destructive and surprising, especially for\rpermission prompts where the click can reject or approve an action.\r\r## Problem 2 — mitigation is undiscoverable, and breaks scrollback in fullscreen\r\r### Mitigation today (and why it's insufficient)\r`CLAUDE_CODE_DISABLE_MOUSE=1` does disable click capture, but:\r- It is an env var only — there is no `settings.json` key, so it is neither\r  persistent per-project/profile nor discoverable.\r- It is not surfaced in `/config` or in the documented settings UI.\r- It is all-or-nothing (also disables click-to-expand, etc.).\r\r### Additional symptom: broken scrollback with `CLAUDE_CODE_DISABLE_MOUSE=1` + fullscreen\rThe fullscreen renderer draws to the alternate screen buffer\r(`\\x1b[?1049h`), which by design has no native scrollback.\r\r- With mouse capture ON, Claude Code intercepts wheel events and scrolls its\r  own internal view, so history is reachable.\r- With `CLAUDE_CODE_DISABLE_MOUSE=1`, wheel events are handed to the terminal.\r  The alternate screen has nothing to scroll, so the terminal surfaces stale\r  main-buffer content instead. The result is that the live conversation\r  appears truncated or \"history lost\" / mixed with old content, even though\r  the conversation data is intact.\r\rIn other words, the two supported settings (fullscreen renderer + disable\rmouse) combine into a confusing, degraded scrollback experience. A user who\rdisables mouse to stop accidental clicks then loses reliable history\rscrolling.\r\r## Requests\r1. A single click on an option should only move focus/highlight; require a\r   second deliberate action (Enter, or a confirm button) before confirming —\r   avoid instant click-to-confirm, especially on permission prompts.\r2. Add a discoverable, persistent setting in `settings.json`\r   (e.g. `\"disableMouse\": true` or `\"mouse\": \"off\"`) rather than env-var only,\r   and optionally surface it in `/config`.\r3. When mouse is disabled under the fullscreen renderer, keep wheel scrolling\r   bound to Claude Code's own internal view (or otherwise prevent the stale\r   main-buffer scrollback), so disabling mouse does not break history.\r4. Document `CLAUDE_CODE_DISABLE_MOUSE=1` (and its interaction with the\r   fullscreen renderer) in the official docs.\r\n\n**Environment Info**\n- Platform: darwin\n- Terminal: iTerm.app\n- Version: 2.1.193\n- Feedback ID: 93aac87b-8ee3-47e5-8f30-021a1049917d\n\n*",
  "labels": ["bug", "has repro", "platform:macos", "area:tui"],
  "state": "open",
  "state_reason": null,
  "created_at": "2026-06-25T23:09:49Z",
  "updated_at": "2026-07-02T23:26:32Z",
  "closed_at": null,
  "reactions_total": 17,
  "comments_count": 4,
  "author": "boaoz-ic",
  "is_maintainer_involved": false,
  "top_comments": [
    {
      "author": "dplate",
      "author_association": "NONE",
      "body": "On Linux in the JetBrains integrated terminal (`TERMINAL_EMULATOR=JetBrains-JediTerm`), `CLAUDE_CODE_DISABLE_MOUSE=1` alone didn't help — the wheel still cycled through `AskUserQuestion` options. There's an additional JediTerm-specific cause beyond click-capture: in the alt-screen buffer with mouse tracking off, JediTerm translates the wheel into cursor ↑/↓ key presses (its built-in `less`/`vim` scroll behavior), so the selector reads them as navigation.\n\nA `settings.json` mitigation that does w",
      "created_at": "2026-06-26T13:56:57Z"
    },
    {
      "author": "karlb379",
      "author_association": "NONE",
      "body": "Running v2.1.195 on Windows Terminal. The new `CLAUDE_CODE_DISABLE_MOUSE_CLICKS` env var partially addresses this, but it also disables clicking to expand/collapse tool output (e.g. \"Ran 2 shell commands\"), which is useful UX.\n\nMy specific pain point: clicking the terminal window to ensure focus before using arrow keys accidentally accepts or denies permission prompts, requiring me to re-prompt Claude to retry the action. A granular option to disable clicks only on interactive prompt elements (y",
      "created_at": "2026-06-27T14:51:11Z"
    },
    {
      "author": "LTurtonVoortman",
      "author_association": "NONE",
      "body": "Same issue within VS2026's terminal, but because the available screen real estate is smaller, accidental clicks are even more frequent than in the windows terminal.",
      "created_at": "2026-07-01T12:40:11Z"
    }
  ]
}
