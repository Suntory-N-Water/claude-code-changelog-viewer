---
title: "Claude Code 週次アップデート (v2.1.217–v2.1.219)"
description: "Claude Opus 5 のデフォルト化やサブエージェントのネスト生成の無効化、/code-review のバックグラウンド実行、絵文字ショートコードの入力補完など、2026年7月20日〜2026年7月26日の Claude Code アップデートから気になった変更をまとめました。"
date: "2026-07-26"
period_start: "2026-07-20"
period_end: "2026-07-26"
total_items: 121
selected_items:
  - id: "aa9fca56a0f0"
    version: "2.1.217"
    comment: "このような遊び心があるアップデートも楽しくていいですね。良くも悪くも機能だけを追加するのがプロダクト開発じゃないようにも、思えました。"
  - id: "6c6cc539386f"
    version: "2.1.217"
    comment: "先週に引き続きですが、今度はサブエージェントのネスト生成がデフォルトで無効化されましたね。\n必然と言うべきでしょうが、これはネストにネストを実行されてしまうと追うことが難しくなったので、正常なアップデートでしょう。\n記載の通り、 `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` で有効化できます。\n\nhttps://claude-code-log.com/reference/settings/claude-code-max-subagent-spawn-depth"
  - id: "25f284352dea"
    version: "2.1.218"
    comment: "記載の通りですが、コードレビューのコマンドをメインのクロードコードで実行されてしまうと、追加の指示ができない問題は確かにありました。\n\n例えば、コードレビューをやっている最中に、「やっぱここも追加して」といったことで、柔軟にレビューの範囲を選択することができると思います。"
  - id: "44446aa010e6"
    version: "2.1.219"
    comment: "Opus 5は 4.8 比較して、細かく指示して動かすモデルから、目的と参考情報を渡せば自律的に判断して進めるモデルへ変わりました。\nとは言いつつ、今までのプロンプト送信のやり方は変わらなそうです。\n私はいつも指示するとき、このような感じで最低限まとめてから記載をしています。従来のプロンプトエンジニアリングが大きく変わることは無さそうですね。\n```\n# 目的（なにをやってほしいのか）\n# 範囲（どこまで対応してよいか）\n# 進め方（どの順番で判断・実行してほしいか）\n# 制約（守ってほしいルール・やってはいけないこと）\n# 完了条件（なにを確認したら完了とするか）\n```\n詳細は公式ドキュメントを見てみましょう！\nhttps://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5"
versions:
  - 2.1.217
  - 2.1.218
  - 2.1.219
---

2026年7月20日~7月26日の変更で、個人的に気になったものをピックアップしました。

今回選んだ4件は、Opus 5 の追加という大きな変更から、絵文字の入力補完のような小さな変更まで幅がありました。サブエージェントがさらにサブエージェントを呼ぶ挙動については、先週に続いて調整が入っています。

## v2.1.217

### プロンプト入力欄で絵文字のショートコード入力によるオートコンプリート機能を追加しました。

> Added emoji shortcode autocomplete in the prompt input: type `:heart:` to insert ❤️, or `:hea` for suggestions — disable with the `emojiCompletionEnabled` setting

これがないと困る場面は正直ないのですが、こういう遊び心のある変更が入っているのを見ると単純に楽しいです。機能を積み上げるだけがプロダクト開発ではないのだな、と改めて思わされました。
<img alt="プロンプト入力欄で絵文字のショートコード入力によるオートコンプリート機能を追加しました。" src="https://assets.claude-code-log.com/weekly/2026-w30/aa9fca56a0f0-20260725-233932.png">

### サブエージェントのネスト生成をデフォルトで無効化しました。

> Changed subagents to no longer spawn nested subagents by default; set `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` to allow deeper nesting

先週も似た調整に触れましたが、サブエージェントがさらにサブエージェントを呼び始めると、どこで何が動いているのか追い切れなくなっていました。デフォルトで止まるのは妥当な判断だと思います。もっと深い階層まで許可したい場合は `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` で調整できます。

https://claude-code-log.com/reference/settings/claude-code-max-subagent-spawn-depth

## v2.1.218

### /code-reviewコマンドがバックグラウンドサブエージェントとして実行されるようになりました。

> Changed `/code-review` to run as a background subagent, so review work no longer fills your conversation and keeps stacked slash commands as its review target

メインのセッションでレビューが走っている間は、こちらから何も言えずに待つしかありませんでした。バックグラウンドに移ったので、レビューの最中に「やっぱりここも見て」と範囲を足せるようになりそうです。待ち時間が読めない作業ほど、こうして切り離せるのはありがたいです。

## v2.1.219

### Claude Opus 5 (`claude-opus-5`) を追加し、新しいデフォルト Opus モデルとしました。100万トークンのコンテキストに対応し、ファストモードは100万トークンあたり$10/$50で利用可能です。

> Added Claude Opus 5 (`claude-opus-5`), now the default Opus model — 1M context, fast mode at $10/$50 per Mtok

Opus 4.8 と比べると、細かく手順を指示して動かすモデルから、目的と参考情報を渡せば自分で判断して進めるモデルへ変わった感触があります。とはいえプロンプトの書き方まで変える必要はなさそうで、私は今も目的・範囲・進め方・制約・完了条件だけは最低限まとめてから渡しています。従来のプロンプトエンジニアリングが無駄になるわけではありません。
<img alt="Claude Opus 5 (`claude-opus-5`) を追加し、新しいデフォルト Opus モデルとしました。100万トークンのコンテキストに対応し、ファストモードは100万トークンあたり$10/$50で利用可能です。" src="https://assets.claude-code-log.com/weekly/2026-w30/44446aa010e6-20260725-232730.png">

https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5

---

最後まで読んでいただきありがとうございました。
今回ピックアップしたのは一部です。全ての変更は公式の CHANGELOG で確認できます。

https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md
