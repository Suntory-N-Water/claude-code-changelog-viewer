---
title: "Claude Code 週次アップデート (v2.1.223–v2.1.225)"
description: "セッション同士でメッセージを送り合える SendMessage と他マシンのセッションへの発信、/review の /code-review への統合、レビューのエフォートレベルの引き継ぎ、1M コンテキスト制限の対象拡大など、2026年8月3日〜2026年8月9日の Claude Code アップデートから気になった変更をまとめました。"
date: "2026-08-09"
period_start: "2026-08-03"
period_end: "2026-08-09"
total_items: 125
selected_items:
  - id: "d10e4607706a"
    version: "2.1.223"
    comment: "Fable5を使っている時に、オートコンパクトバッファーが設定されていなかったような気がします。これによって長時間のタスクをやった際にも、サブエージェントへ分割させているものの、その報告によってトークンが芋づる式に溜まっていき、最終的には99%までいったことがありました。現在確認してみると、Fable5でも想定通りオートコンパクトバッファーが設定されているため、今後はこのようなことが発生しないと思われます。"
  - id: "c8c8345f148f"
    version: "2.1.223"
    comment: "私もこれ知らなかったんですが、プルリクエストレビューとコードレビューのスラッシュコマンドの違いは、記載してある通り、プルリクエストを単発でレビューするんだったら「レビュー」、そしていろんなパラメーターを実行して変更するためには「code-review」が分かれていたんですね。実際、これ私がレビューするときにどっち使えばいいんだっけと、またコードで公式ドキュメントを見に行ったような気がします。\n\n今後はコードレビューの中でPRを指定したり、レベルを指定したりなど、引数で動作することになり、統合されたので、コードレビューを実施する側はかなりシンプルな作りになっています。\n\n余談ですが、これとは別に `/simplify` というコマンドもあります。\nこちらは「直近のコード変更を、再利用性・簡潔さ・効率性・抽象化の4観点で自動レビューし、問題があれば修正まで適用してくれる」機能です。コードのレビューだったら、コードレビュー コマンドを使います。コードの内容を効率的や簡潔にしたいんだったら、こちらのコマンドというふうに分かれているのは覚えておきましょう。"
  - id: "0a3c18a5e77d"
    version: "2.1.223"
    comment: "地味な変更ですが、私はよくcode-reviewで実施することが多いので、毎回毎回指定する必要がなくなったのは助かります。"
  - id: "3f789c54fa94"
    version: "2.1.224"
    comment: "新機能として、別のターミナルに対してClaude自身がコメントや進捗度を確認するような仕組みが確立されました。\n\n公式がこのように発言している通り、今実施しているタスクなどを引き継ぐ時とかに有効です。\n```\nNew in Claude Code: your sessions can now message each other.\n\nInstead of having to re-explain yourself in another session, you can now tell Claude to do it. It sends a summary (not your history or files), and the other session picks it up mid-task.\n```\n\n例えばセッションAでフロントエンドの位置機能を実装した後、セッションBで新しく修正をするときには、大掛かりな引き継ぎ文章を作成しないといけません。そうではなく、ClaudeがClaude自身に引き継ぎをシームレスに行うことができたのは、ユーザーから見てもかなり楽です。\n\n私はいつも「次のセッションの指示を簡潔に考えて、pbcopyしてクリップボードに入れてください」とやっていますが、これをする必要が一切なくなるのは、かなり開発体験としては楽になるのかなと思います。\n公式が推奨しているのは「現在動いているセッションで新たに発見したことを別のセッションに共有する」用途です。これがあると、人間が別のセッションで気づいた破壊的な変更などを、並列で動いている別のセッションに連携することができるので、人間がわざわざ介入することなく、Claudeが自律的に進めてくれます。"
  - id: "6988eb10f2e1"
    version: "2.1.225"
    comment: "Web版のClaude Codeのセッションに対して接続できるようになりました。v2.1.224 ではローカルにある別のセッションに指示することができていましたが、今回のアップデートで他マシン・Web のセッションに対して「自分から話しかけられる」ようになりました。\n\n以前は、向こうから先にメッセージが来ていないと何もできませんでした。届いたメッセージに返信することしかできず、こちらから切り出すことは不可能でした。今回それが取り払われて、`ListAgents` に出ている名前を指定するだけで、いきなり第一声を送れるようになりました。"
versions:
  - 2.1.223
  - 2.1.224
  - 2.1.225
---

2026年8月3日~8月9日の変更で、個人的に気になったものをピックアップしました。

7月25日から8月の頭まで、10日ほど Claude Code の更新が止まっていました。過去に同じくらい間が空いたときは、再開後の1バージョンに100件以上の変更が詰め込まれていましたが、今回はそこまでではありませんでしたね。

Codex や Grok、Kimi、DeepSeek と、コーディングエージェントの競合はこれまで以上に増えています。
この10日間、Anthropic は裏側の整備に時間を使っていたのでしょうか。競争相手がいてくれるおかげで利用者はより良いものを使えるので、Anthropic にも他のエージェントにも頑張ってほしいという複雑な気持ちになります。

話が逸れました。今回選んだのは、セッション同士でメッセージを送り合える新機能とその続き、そしてコードレビューのコマンドが1本にまとまった変更です。

## v2.1.223

### 環境変数 `CLAUDE_CODE_DISABLE_1M_CONTEXT` を、特定の固定モデルリストだけでなくネイティブで 1M トークン窓を持つすべての Claude モデルを自動要約(auto-compaction)により 200K トークンに制限する挙動に変更し、要約によって 200K 未満に保てない場合は起動時に警告を表示するようにしました。

> Changed `CLAUDE_CODE_DISABLE_1M_CONTEXT` to hold every Claude model with a native 1M window to 200K via auto-compaction, not just a fixed list; a startup warning now appears when auto-compaction isn't holding the session to 200K

Fable 5 で長時間のタスクを回していたときのことです。サブエージェントに分けて進めていたのに、その報告が戻るたびにコンテキストが埋まっていき、気づいたら 99% でした。
自動要約のバッファが効いていないのではと疑っていたのですが、いま確認すると Fable 5 でも想定通りに効いています。
<img alt="環境変数 `CLAUDE_CODE_DISABLE_1M_CONTEXT` を、特定の固定モデルリストだけでなくネイティブで 1M トークン窓を持つすべての Claude モデルを自動要約(auto-compaction)により 200K トークンに制限する挙動に変更し、要約によって 200K 未満に保てない場合は起動時に警告を表示するようにしました。" src="https://assets.claude-code-log.com/weekly/2026-w32/d10e4607706a-20260807-233205.png">

### `/review` コマンドを、ローカルの差分またはプルリクエストの検証を統合して行う `/code-review` コマンドのエイリアスへと変更しました。また、高度なクラウドベースのコードレビューには `/code-review ultra` を使用するように案内を変更しました。

> Changed `/review` to be an alias of `/code-review`, which reviews the current diff or a PR (`/code-review <level> <pr#>`); use `/code-review ultra` for a deep cloud review

レビューを頼みたいときに `/review` と `/code-review` のどちらだったか毎回思い出せず、公式ドキュメントを見に行っていました。1つに寄せてもらえると、この迷いがなくなります。なお `/simplify` は再利用性や簡潔さの観点でコードを整えてくれる別のコマンドなので、レビューしたいときは `/code-review` を使います。

### エフォートレベルを指定せずに `/code-review` を実行した際、前回指定して実行したエフォートレベルが自動的に再利用されるように仕様を変更しました(変更したい場合は `/code-review high` のように指定します)。

> Changed `/code-review` with no effort level to reuse the level you typed last; type a level like `/code-review high` to change it

私は `/code-review` をかなりの頻度で実行するので、そのたびに `high` を打ち直していました。地味な変更ですが、前回のレベルを覚えていてくれるだけで助かります。

## v2.1.224

### セッションをまたぐ`SendMessage`のサポートを追加：同じ開発者の複数のマシン間でClaude Codeのセッション同士が互いにメッセージを送受信できるようになり、`ListAgents`コマンドを使用してそれらを発見できます(macOSおよびLinux)。

> Added cross-session `SendMessage`: Claude Code sessions can now message each other, on any of your machines, with `ListAgents` to discover them (macOS and Linux)

私はいつも「次のセッションへの指示を簡潔にまとめて pbcopy してください」と頼んで、クリップボード経由で引き継いでいました。これが要らなくなるだけでもかなり楽になります。
その他、並行して動かしている別のセッションに、こちらで見つけた破壊的な変更をそのまま伝えられるので、人間が仲介せずに済むところに期待しています。
<img alt="セッションをまたぐ`SendMessage`のサポートを追加：同じ開発者の複数のマシン間でClaude Codeのセッション同士が互いにメッセージを送受信できるようになり、`ListAgents`コマンドを使用してそれらを発見できます(macOSおよびLinux)。" src="https://assets.claude-code-log.com/weekly/2026-w32/3f789c54fa94-20260807-232701.png">

https://code.claude.com/docs/en/cross-session-messaging

https://x.com/ClaudeDevs/status/2085817074816070014?s=20

## v2.1.225

### `SendMessage`ツールを拡張し、他マシンで稼働中のRemote Controlセッションに対して名前で直接会話を開始できるようにしました(これまでは受信後の返信のみ可能で、`ListAgents`で`name [ref]`として表示されます)。

> SendMessage can now start a conversation with your Remote Control sessions on other machines by name (`ListAgents` shows them as `name [ref]`), instead of only replying after they message you first

v2.1.224 の時点では、このマシンの外にいるセッションには届いたメッセージに返信することしかできず、こちらから話を切り出せませんでした。試しに Mac のローカルセッションから Web 版のセッションへ第一声を送ってみたところ、Remote Control 経由できちんと届きました。`ListAgents` に出ている名前を指定するだけで済むので、放置しているセッションにも自分のタイミングで指示を投げられます。
<img alt="`SendMessage`ツールを拡張し、他マシンで稼働中のRemote Controlセッションに対して名前で直接会話を開始できるようにしました(これまでは受信後の返信のみ可能で、`ListAgents`で`name [ref]`として表示されます)。" src="https://assets.claude-code-log.com/weekly/2026-w32/6988eb10f2e1-20260808-073935.png">

---

最後まで読んでいただきありがとうございました。
今回ピックアップしたのは一部です。全ての変更は公式の CHANGELOG で確認できます。

https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md
