{
  "id": "20260727-183300",
  "timestamp": "2026-07-27T18:33:00.953Z",
  "aiSummary": "Python SDKにおいて会話の終了理由を特定するterminal_reasonや、モデル使用量の詳細な型定義が追加されました。GNU screen利用時のクリップボード同期改善や、サブエージェント実行時におけるフックの動作仕様、およびHTTPフックのホワイトリスト制御が明確化されています。ネットワーク制限を強化するstrictAllowlist設定が導入され、承認プロンプトを出さずに外部アクセスを拒否する運用が可能になりました。リモートコントロール機能の制限対象となる条件にカスタムのベースURLが含まれることが明記され、トラブルシューティング情報が拡充されています。",
  "files": [
    {
      "filename": "agent-sdk/python.md",
      "additions": 7,
      "deletions": 3,
      "explanation": "会話の終了理由を示すterminal_reasonフィールドと、モデル使用状況を詳細に定義するModelUsage型が追加されました。",
      "hunks": [
        {
          "header": "@@ -1600,22 +1600,24 @@ class ResultMessage:",
          "lines": [
            {
              "type": "context",
              "content": "    usage: dict[str, Any] | None = None"
            },
            {
              "type": "context",
              "content": "    result: str | None = None"
            },
            {
              "type": "context",
              "content": "    structured_output: Any = None"
            },
            {
              "type": "removed",
              "content": "    model_usage: dict[str, Any] | None = None"
            },
            {
              "type": "added",
              "content": "    model_usage: dict[str, ModelUsage] | None = None"
            },
            {
              "type": "context",
              "content": "    permission_denials: list[Any] | None = None"
            },
            {
              "type": "context",
              "content": "    deferred_tool_use: DeferredToolUse | None = None"
            },
            {
              "type": "context",
              "content": "    errors: list[str] | None = None"
            },
            {
              "type": "context",
              "content": "    api_error_status: int | None = None"
            },
            {
              "type": "context",
              "content": "    uuid: str | None = None"
            },
            {
              "type": "added",
              "content": "    terminal_reason: str | None = None"
            },
            {
              "type": "context",
              "content": "```"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "The `subtype` field determines which other fields are populated. It is one of `\"success\"`, `\"error_during_execution\"`, `\"error_max_turns\"`, `\"error_max_budget_usd\"`, or `\"error_max_structured_output_retries\"`. The Python dataclass flattens all variants into one shape, so fields that don't apply to the returned subtype are `None`."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "removed",
              "content": "Several fields carry diagnostic detail when the conversation ends on an error:"
            },
            {
              "type": "added",
              "content": "Several fields carry diagnostic detail about how the conversation ended:"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "- `is_error`: `True` when the conversation ended in an error state. Always `True` on the `error_*` subtypes. On `subtype=\"success\"` it is `True` when the final model request failed, meaning the agent loop completed but the last API call returned an error."
            },
            {
              "type": "context",
              "content": "- `api_error_status`: the HTTP status code of the terminating API error. `None` when the turn ended without one. Populated only on `subtype=\"success\"`."
            },
            {
              "type": "context",
              "content": "- `result`: text of the final assistant message on `subtype=\"success\"`, or `None` on the `error_*` subtypes. When `subtype=\"success\"` and `is_error=True`, this holds the API error string if one is available but can be empty, so check `api_error_status` and the preceding `AssistantMessage` content for detail."
            },
            {
              "type": "context",
              "content": "- `errors`: loop-level error strings such as the max-turns message. Populated only on the `error_*` subtypes."
            },
            {
              "type": "added",
              "content": "- `terminal_reason`: why the query loop terminated, such as `\"completed\"`, `\"max_turns\"`, or `\"aborted_streaming\"`. A value of `\"aborted_streaming\"` or `\"aborted_tools\"` means the turn was cancelled by an interrupt. `None` when the CLI didn't report a terminal reason, such as with an older CLI version. See [`SDKResultMessage`](/docs/en/agent-sdk/typescript#sdkresultmessage) for the full list of values."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "The `usage` dict contains the following keys when present:"
            },
            {
              "type": "context",
              "content": ""
            }
          ]
        },
        {
          "header": "@@ -1626,7 +1628,7 @@ The `usage` dict contains the following keys when present:",
          "lines": [
            {
              "type": "context",
              "content": "| `cache_creation_input_tokens` | `int` | Tokens used to create new cache entries. |"
            },
            {
              "type": "context",
              "content": "| `cache_read_input_tokens` | `int` | Tokens read from existing cache entries. |"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "removed",
              "content": "The `model_usage` dict maps model names to per-model usage. The inner dict keys use camelCase because the value is passed through unmodified from the underlying CLI process, matching the TypeScript [`ModelUsage`](/docs/en/agent-sdk/typescript#modelusage) type:"
            },
            {
              "type": "added",
              "content": "The `model_usage` dict maps model names to per-model usage. Each value is a `ModelUsage` TypedDict whose keys use camelCase, because the value is passed through unmodified from the underlying CLI process. Import via `from claude_agent_sdk.types import ModelUsage`. The keys are:"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "| Key | Type | Description |"
            },
            {
              "type": "context",
              "content": "| - | - | - |"
            }
          ]
        },
        {
          "header": "@@ -1638,6 +1640,8 @@ The `model_usage` dict maps model names to per-model usage. The inner dict keys",
          "lines": [
            {
              "type": "context",
              "content": "| `costUSD` | `float` | Estimated cost in USD for this model, computed client-side. See [Track cost and usage](/docs/en/agent-sdk/cost-tracking) for billing caveats. |"
            },
            {
              "type": "context",
              "content": "| `contextWindow` | `int` | Context window size for this model. |"
            },
            {
              "type": "context",
              "content": "| `maxOutputTokens` | `int` | Maximum output token limit for this model. |"
            },
            {
              "type": "added",
              "content": "| `canonicalModel` | `str` | Canonical model ID used for the pricing lookup. May differ from the raw model string the entry is keyed by, such as a provider-specific ID or alias. Not always present. |"
            },
            {
              "type": "added",
              "content": "| `provider` | `str` | API provider that served this model, such as `firstParty`, `bedrock`, `vertex`, `foundry`, `anthropicAws`, `mantle`, or `gateway`. Not always present. |"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "### `StreamEvent`"
            },
            {
              "type": "context",
              "content": ""
            }
          ]
        }
      ]
    },
    {
      "filename": "fullscreen.md",
      "additions": 1,
      "deletions": 1,
      "explanation": "GNU screen環境下でのクリップボード同期挙動が改善され、長文コピー時のベース64出力問題に関する注意書きが追加されました。",
      "hunks": [
        {
          "header": "@@ -177,7 +177,7 @@ Claude Code writes the selection to your system clipboard, and the path it uses",
          "lines": [
            {
              "type": "context",
              "content": "- **Linux**: `wl-copy` on Wayland, or `xclip` or `xsel` on X11, whichever is installed. Claude Code writes both the clipboard and the PRIMARY selection, so middle-click paste works."
            },
            {
              "type": "context",
              "content": "- **Windows and WSL**: PowerShell `Set-Clipboard`"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "removed",
              "content": "Inside tmux it also writes to the tmux paste buffer. Over SSH it falls back to OSC 52 escape sequences. Claude Code prints a toast after each copy telling you which path it used."
            },
            {
              "type": "added",
              "content": "Inside tmux it also writes to the tmux paste buffer. Over SSH it falls back to OSC 52 escape sequences. Inside GNU screen, Claude Code copies long selections to the clipboard too. Before v2.1.219, if you copied a selection longer than roughly 570 characters, GNU screen printed base64 text into the window instead. Claude Code prints a toast after each copy telling you which path it used."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "Some terminals block OSC 52 by default. iTerm2 blocks it until you turn on Settings → General → Selection → Applications in terminal may access clipboard; running [`/terminal-setup`](/docs/en/terminal-config) in iTerm2 enables this for you."
            },
            {
              "type": "context",
              "content": ""
            }
          ]
        }
      ]
    },
    {
      "filename": "hooks.md",
      "additions": 9,
      "deletions": 0,
      "explanation": "サブエージェントでのフック実行ルールや、設定レベル間でのマージ仕様、HTTPフックのURLおよび環境変数の制限方法が追記されました。",
      "hunks": [
        {
          "header": "@@ -159,8 +159,17 @@ Where you define a hook determines its scope:",
          "lines": [
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "For details on settings file resolution, see [settings](/docs/en/settings)."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "added",
              "content": "Hooks from settings files, managed policy settings, and plugins also run inside [subagents](/docs/en/sub-agents). When a subagent calls a tool, tool events such as `PreToolUse` and `PostToolUse` fire the same configured hooks as in the main conversation, and the input carries the `agent_id` and `agent_type` [common input fields](#common-input-fields) that identify the subagent."
            },
            {
              "type": "added",
              "content": ""
            },
            {
              "type": "context",
              "content": "Enterprise administrators can use `allowManagedHooksOnly` to block user, project, and plugin hooks. Hooks from plugins force-enabled in managed settings `enabledPlugins` are exempt, so administrators can distribute vetted hooks through an organization marketplace. See [Hook configuration](/docs/en/settings#hook-configuration)."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "added",
              "content": "Hook entries merge across settings levels rather than replacing each other: user, project, and local settings add their own hooks without removing managed ones, and the [`disableAllHooks`](#disable-or-remove-hooks) setting can't disable managed hooks from outside managed settings."
            },
            {
              "type": "added",
              "content": ""
            },
            {
              "type": "added",
              "content": "The [HTTP hook allowlists](/docs/en/settings#hook-configuration) apply to hooks from every source, including managed policy settings:"
            },
            {
              "type": "added",
              "content": ""
            },
            {
              "type": "added",
              "content": "- `allowedHttpHookUrls`: when defined at any settings level, Claude Code runs an HTTP hook handler only if its URL matches the merged allowlist"
            },
            {
              "type": "added",
              "content": "- `httpHookAllowedEnvVars`: when defined, Claude Code interpolates only the environment variables on that list into hook headers"
            },
            {
              "type": "added",
              "content": ""
            },
            {
              "type": "context",
              "content": "### Matcher patterns"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "The `matcher` field filters when hooks fire. How a matcher is evaluated depends on the characters it contains:"
            }
          ]
        }
      ]
    },
    {
      "filename": "remote-control.md",
      "additions": 3,
      "deletions": 1,
      "explanation": "カスタムベースURL使用時の制限に関するエラーメッセージの改善内容と、プロバイダー変数の確認方法が詳しく説明されています。",
      "hunks": [
        {
          "header": "@@ -267,7 +267,9 @@ Claude Code could not reach the feature-flag service to check whether Remote Con",
          "lines": [
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "### \"Remote Control is only available when using Claude via api.anthropic.com\""
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "removed",
              "content": "The session isn't talking to the Anthropic API directly, so there is no claude.ai backend to pair with. This happens on Amazon Bedrock, Google Cloud's Agent Platform, and Microsoft Foundry. As of v2.1.196 it also happens when [`ANTHROPIC_BASE_URL`](/docs/en/env-vars) points at a host other than `api.anthropic.com`, such as an [LLM gateway](/docs/en/llm-gateway) or proxy, even if you sign in with claude.ai. Unset `ANTHROPIC_BASE_URL` and restart the session to use Remote Control."
            },
            {
              "type": "added",
              "content": "The session isn't talking to the Anthropic API directly, so there is no claude.ai backend to pair with. This happens on Amazon Bedrock, Google Cloud's Agent Platform, and Microsoft Foundry. It also happens when [`ANTHROPIC_BASE_URL`](/docs/en/env-vars) points at a host other than `api.anthropic.com`, such as an [LLM gateway](/docs/en/llm-gateway) or proxy, even if you sign in with claude.ai. Before v2.1.196, Claude Code didn't show this message for a custom `ANTHROPIC_BASE_URL`. See the [error reference](/docs/en/errors#remote-control-requires-the-anthropic-api) for the full cause list."
            },
            {
              "type": "added",
              "content": ""
            },
            {
              "type": "added",
              "content": "The message names what routed the session away from the Anthropic API, such as `CLAUDE_CODE_USE_BEDROCK` or a custom `ANTHROPIC_BASE_URL`. If you have an eligible claude.ai login, unset the named variable, remove it from the `env` key in [settings](/docs/en/settings) if you set it there, and restart the session. Before v2.1.219, the message was only the sentence in this section's header, so on older versions check your environment yourself for provider variables such as `CLAUDE_CODE_USE_BEDROCK` and `CLAUDE_CODE_USE_VERTEX`, and for `ANTHROPIC_BASE_URL`."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "### \"Remote Control is disabled by your organization's policy\""
            },
            {
              "type": "context",
              "content": ""
            }
          ]
        }
      ]
    },
    {
      "filename": "sandboxing.md",
      "additions": 1,
      "deletions": 0,
      "explanation": "ホワイトリスト外の通信をプロンプトなしで遮断するstrictAllowlist設定の動作仕様と、設定可能な場所が定義されました。",
      "hunks": [
        {
          "header": "@@ -294,6 +294,7 @@ You can grant write access to additional paths using `sandbox.filesystem.allowWr",
          "lines": [
            {
              "type": "context",
              "content": "Network access is controlled through a proxy server running outside the sandbox:"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "- **Domain restrictions**: no domains are pre-allowed by default. The first time a command needs a new domain, Claude Code prompts for approval. As of v2.1.191, choosing Yes allows the host for the rest of the current session, so later connections to the same host do not prompt again. Pre-allow domains with [`allowedDomains`](/docs/en/settings#sandbox-settings) to avoid the prompt entirely. `WebFetch` allow rules also pre-allow domains, as described in [Permission rules](#permission-rules)."
            },
            {
              "type": "added",
              "content": "- **Strict allowlist**: if you set [`strictAllowlist`](/docs/en/settings#sandbox-settings) to `true` in user, managed, or CLI `--settings` settings, Claude Code denies sandboxed commands access to any host outside the allowlist instead of prompting. The allowlist is the same one the sandbox otherwise prompts against: `allowedDomains` plus domains from `WebFetch(domain:...)` allow rules, or only the managed settings entries when `allowManagedDomainsOnly` is set. Claude Code enforces this for sandboxed commands only; in-process tools such as `WebFetch` still follow their [permission rules](#permission-rules). Setting it in a repository's `.claude/settings.json` or `.claude/settings.local.json` has no effect. Requires Claude Code v2.1.219 or later."
            },
            {
              "type": "context",
              "content": "- **Managed lockdown**: if [`allowManagedDomainsOnly`](/docs/en/settings#sandbox-settings) is set in managed settings, non-allowed domains are blocked automatically instead of prompting, and only `allowedDomains` and `WebFetch(domain:...)` allow rules from managed settings are honored."
            },
            {
              "type": "context",
              "content": "- **Custom proxy support**: advanced users can implement custom rules on outgoing traffic"
            },
            {
              "type": "context",
              "content": "- **Comprehensive coverage**: restrictions apply to all scripts, programs, and subprocesses spawned by commands"
            }
          ]
        }
      ]
    },
    {
      "filename": "settings.md",
      "additions": 1,
      "deletions": 0,
      "explanation": "ネットワーク分離を強化するnetwork.strictAllowlistオプションが設定項目の一覧に追加され、v2.1.219以降で利用可能であることが示されています。",
      "hunks": [
        {
          "header": "@@ -416,6 +416,7 @@ Configure advanced sandboxing behavior. Sandboxing isolates bash commands from y",
          "lines": [
            {
              "type": "context",
              "content": "| `network.allowMachLookup` | Additional XPC/Mach service names the sandbox may look up (macOS only). Supports a single trailing `*` for prefix matching. Needed for tools that communicate via XPC such as the iOS Simulator or Playwright. | `[\"com.apple.coresimulator.*\"]` |"
            },
            {
              "type": "context",
              "content": "| `network.allowedDomains` | Array of domains to allow for outbound network traffic. Supports wildcards (e.g., `*.example.com`). | `[\"github.com\", \"*.npmjs.org\"]` |"
            },
            {
              "type": "context",
              "content": "| `network.deniedDomains` | Array of domains to block for outbound network traffic. Supports the same wildcard syntax as `allowedDomains`. Takes precedence over `allowedDomains` when both match. Merged from all settings sources regardless of `allowManagedDomainsOnly`. | `[\"sensitive.cloud.example.com\"]` |"
            },
            {
              "type": "added",
              "content": "| `network.strictAllowlist` | Deny sandboxed commands access to hosts outside the allowlist instead of prompting for approval. The allowlist is `allowedDomains` plus domains from `WebFetch(domain:...)` allow rules, or only the managed settings entries when `allowManagedDomainsOnly` is set. Enforced for sandboxed commands only; in-process tools such as `WebFetch` aren't gated by this setting. Only honored from user, managed, or CLI `--settings` settings, not from `.claude/settings.json` or `.claude/settings.local.json`. Default: false. Requires Claude Code v2.1.219 or later. See [Network isolation](/docs/en/sandboxing#network-isolation) | `true` |"
            },
            {
              "type": "context",
              "content": "| `network.allowManagedDomainsOnly` | (Managed settings only) Only `allowedDomains` and `WebFetch(domain:...)` allow rules from managed settings are respected. Domains from user, project, and local settings are ignored. Non-allowed domains are blocked automatically without prompting the user. Denied domains are still respected from all sources. Default: false | `true` |"
            },
            {
              "type": "context",
              "content": "| `network.httpProxyPort` | HTTP proxy port used if you wish to bring your own proxy. If not specified, Claude will run its own proxy. | `8080` |"
            },
            {
              "type": "context",
              "content": "| `network.socksProxyPort` | SOCKS5 proxy port used if you wish to bring your own proxy. If not specified, Claude will run its own proxy. | `8081` |"
            }
          ]
        }
      ]
    },
    {
      "filename": "sub-agents.md",
      "additions": 3,
      "deletions": 1,
      "explanation": "サブエージェントによるツール呼び出し時にも、メインセッションと共通のフックが発火する仕組みについて解説が更新されました。",
      "hunks": [
        {
          "header": "@@ -563,7 +563,9 @@ See [Permissions documentation](/docs/en/permissions#tool-specific-permission-ru",
          "lines": [
            {
              "type": "context",
              "content": "Subagents can define [hooks](/docs/en/hooks) that run during the subagent's lifecycle. There are two ways to configure hooks:"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "- **In the subagent's frontmatter**: define hooks that run only while that subagent is active"
            },
            {
              "type": "removed",
              "content": "- **In `settings.json`**: define hooks that run in the main session when subagents start or stop"
            },
            {
              "type": "added",
              "content": "- **In `settings.json`**: define session-wide hooks that also fire inside subagents. Tool events such as `PreToolUse` and `PostToolUse` fire for the subagent's tool calls the same way they do in the main conversation, and `SubagentStart` and `SubagentStop` fire when a subagent starts or finishes"
            },
            {
              "type": "added",
              "content": ""
            },
            {
              "type": "added",
              "content": "Hooks from [settings files, managed policy settings, and plugins](/docs/en/hooks#hook-locations) all apply inside subagents, so a `PreToolUse` hook in `settings.json` also runs before every tool a subagent uses."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "#### Hooks in subagent frontmatter"
            },
            {
              "type": "context",
              "content": ""
            }
          ]
        }
      ]
    }
  ]
}
