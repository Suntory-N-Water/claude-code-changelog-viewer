{
  "id": "20260703-154028",
  "timestamp": "2026-07-03T15:40:28.247Z",
  "aiSummary": "CLAUDE_CODE_ATTRIBUTION_HEADER環境変数によるアトリビューションブロックの除外と、プロンプトキャッシュへの影響に関する記述が詳細化されました。LLMゲートウェイを利用する際にシステムプロンプトの配列形式を維持するための具体的な要件が追加されています。また、パーミッション設定におけるパスの解決基準が、設定ファイルの保存場所を起点とする仕様に明確化されました。",
  "files": [
    {
      "filename": "env-vars.md",
      "additions": 1,
      "deletions": 1,
      "explanation": "アトリビューションヘッダーを無効化した際、Anthropic APIへの直接接続時はキャッシュに影響しない旨の表現がより正確に修正されました。",
      "hunks": [
        {
          "header": "@@ -144,7 +144,7 @@ Claude Code reads environment variables at startup, so changes take effect the n",
          "lines": [
            {
              "type": "context",
              "content": "| `CLAUDE_CODE_ALWAYS_ENABLE_EFFORT` | Set to `1` to send the [effort](/en/model-config#adjust-effort-level) parameter with every request, even when Claude Code does not recognize the model ID as effort-capable. Use this when routing through an [LLM gateway](/en/llm-gateway) or third-party provider that serves models under custom identifiers. Models that reject the effort parameter at the API, including Claude 3 models, Sonnet 4.0 and 4.5, Opus 4.0 and 4.1, and Haiku 4.5, are still excluded so requests do not fail |"
            },
            {
              "type": "context",
              "content": "| `CLAUDE_CODE_API_KEY_HELPER_TTL_MS` | Interval in milliseconds at which credentials should be refreshed (when using [`apiKeyHelper`](/en/settings#available-settings)) |"
            },
            {
              "type": "context",
              "content": "| `CLAUDE_CODE_ARTIFACT_AUTO_OPEN` | Set to `0` to stop Claude Code from opening the browser automatically when a new [artifact](/en/artifacts) is published. Republishing an existing artifact does not open the browser regardless of this setting |"
            },
            {
              "type": "removed",
              "content": "| `CLAUDE_CODE_ATTRIBUTION_HEADER` | Set to `0` to omit the attribution block (client version and prompt fingerprint) from the start of the system prompt. Disabling it improves prompt-cache hit rates when routing through an [LLM gateway](/en/llm-gateway). Anthropic API caching is unaffected |"
            },
            {
              "type": "added",
              "content": "| `CLAUDE_CODE_ATTRIBUTION_HEADER` | Set to `0` to omit the attribution block (client version and prompt fingerprint) from the start of the system prompt. Disabling it improves prompt-cache hit rates when routing through an [LLM gateway](/en/llm-gateway). Caching on a direct connection to the Anthropic API is unaffected either way |"
            },
            {
              "type": "context",
              "content": "| `CLAUDE_CODE_AUTO_COMPACT_WINDOW` | Set the context capacity in tokens used for auto-compaction calculations. Defaults to the model's context window, 200K for standard models or 1M for [extended context](/en/model-config#extended-context) models, except on Sonnet 5, which has its own [default threshold](/en/model-config#sonnet-5-context-window). Use a lower value like `500000` on a 1M model to treat the window as 500K for compaction purposes. The value is capped at the model's actual context window. `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE` is applied as a percentage of this value. Setting this variable decouples the compaction threshold from the status line's `used_percentage`, which always uses the model's full context window |"
            },
            {
              "type": "context",
              "content": "| `CLAUDE_CODE_AUTO_CONNECT_IDE` | Override automatic [IDE connection](/en/vs-code). By default, Claude Code connects automatically when launched inside a supported IDE's integrated terminal. Set to `false` to prevent this. Set to `true` to force a connection attempt when auto-detection fails, such as when tmux obscures the parent terminal. Takes precedence over the [`autoConnectIde`](/en/settings#global-config-settings) global config setting |"
            },
            {
              "type": "context",
              "content": "| `CLAUDE_CODE_BRIDGE_SESSION_ID` | Set automatically in Bash tool and [hook command](/en/hooks) subprocesses while the session has an active [Remote Control](/en/remote-control) connection, and removed when the connection ends. The value is the session's ID in `session_` form, the same identifier that appears in the session's `claude.ai/code` URL, so a script can link back to the session that ran it. Requires Claude Code v2.1.199 or later. In [cloud sessions](/en/claude-code-on-the-web), read `CLAUDE_CODE_REMOTE_SESSION_ID` instead |"
            }
          ]
        }
      ]
    },
    {
      "filename": "llm-gateway-protocol.md",
      "additions": 9,
      "deletions": 1,
      "explanation": "ゲートウェイがシステムプロンプトを処理する際、配列の順序変更や文字列結合を行うとキャッシュ効率が低下する問題と、その回避策が追記されました。",
      "hunks": [
        {
          "header": "@@ -89,7 +89,15 @@ The exception is a non-Anthropic upstream such as Bedrock or Agent Platform, whe",
          "lines": [
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "## System prompt attribution block"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "removed",
              "content": "Claude Code prepends a short attribution block to the system prompt containing the client version and a fingerprint derived from the conversation. The `api.anthropic.com` endpoint strips the block before processing, so it doesn't affect first-party prompt caching; any other upstream receives it as part of the prompt. Anthropic and the cloud providers' Claude endpoints read it for attribution, so to omit it set [`CLAUDE_CODE_ATTRIBUTION_HEADER=0`](/en/env-vars) rather than stripping it in the gateway."
            },
            {
              "type": "added",
              "content": "Claude Code prepends a short attribution block to the system prompt containing the client version and a fingerprint derived from the conversation. The `api.anthropic.com` endpoint strips the block before processing when it arrives unchanged as the first system block, so it doesn't affect first-party prompt caching. Any other upstream receives it as part of the prompt."
            },
            {
              "type": "added",
              "content": ""
            },
            {
              "type": "added",
              "content": "The strip is positional, so it only works when the gateway forwards the `system` array unchanged. To keep the block out of the prompt without losing other system content:"
            },
            {
              "type": "added",
              "content": ""
            },
            {
              "type": "added",
              "content": "- Forward the `system` array exactly as received, keeping the block first: prepending another system block, reordering the array, or converting it to a single string defeats the strip, and the block then reaches the model and the prompt cache key."
            },
            {
              "type": "added",
              "content": "- Keep the block in its own array entry: the endpoint treats a merged block that starts with the attribution header as attribution in its entirety and drops everything merged into it, including the rest of the system prompt."
            },
            {
              "type": "added",
              "content": "- If your gateway must reshape system content, set [`CLAUDE_CODE_ATTRIBUTION_HEADER=0`](/en/env-vars) so Claude Code omits the block. Anthropic and the cloud providers' Claude endpoints read the block for attribution, so omit it at the client rather than stripping or moving it in the gateway."
            },
            {
              "type": "added",
              "content": ""
            },
            {
              "type": "added",
              "content": "Requests that reach the endpoint unmodified are unaffected."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "From Claude Code v2.1.181, the block is stable for the lifetime of a conversation when requests route through a custom base URL, so a gateway-side prompt cache keyed on the full request body works without disabling it. Before v2.1.181 the block included a per-request token; on those versions, set `CLAUDE_CODE_ATTRIBUTION_HEADER=0` if your gateway implements such a cache."
            },
            {
              "type": "context",
              "content": ""
            }
          ]
        }
      ]
    },
    {
      "filename": "permissions.md",
      "additions": 13,
      "deletions": 2,
      "explanation": "スラッシュで始まるパス設定がプロジェクトルートではなく、設定ファイル自体の場所を起点に解決される仕様と、各設定レベルごとの解決例が追加されました。",
      "hunks": [
        {
          "header": "@@ -234,10 +234,21 @@ Read and Edit rules both follow the [gitignore](https://git-scm.com/docs/gitigno",
          "lines": [
            {
              "type": "context",
              "content": "| - | - | - | - |"
            },
            {
              "type": "context",
              "content": "| `//path` | Absolute path from filesystem root | `Read(//Users/alice/secrets/**)` | `/Users/alice/secrets/**` |"
            },
            {
              "type": "context",
              "content": "| `~/path` | Path from home directory | `Read(~/Documents/*.pdf)` | `/Users/alice/Documents/*.pdf` |"
            },
            {
              "type": "removed",
              "content": "| `/path` | Path relative to project root | `Edit(/src/**/*.ts)` | `<project root>/src/**/*.ts` |"
            },
            {
              "type": "added",
              "content": "| `/path` | Path relative to the settings source | `Edit(/src/**/*.ts)` | `<project root>/src/**/*.ts` in project settings |"
            },
            {
              "type": "context",
              "content": "| `path` or `./path` | Path relative to current directory | `Read(*.env)` | `<cwd>/*.env` |"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "removed",
              "content": "A pattern like `/Users/alice/file` isn't an absolute path. It's relative to the project root. Use `//Users/alice/file` for absolute paths."
            },
            {
              "type": "added",
              "content": "A pattern like `/Users/alice/file` isn't an absolute path. The single leading slash anchors at the settings source, not the filesystem root. Use `//Users/alice/file` for absolute paths."
            },
            {
              "type": "added",
              "content": ""
            },
            {
              "type": "added",
              "content": "A `/path` pattern anchors at the directory associated with the settings file that defines it, so the same rule matches different locations depending on where you put it:"
            },
            {
              "type": "added",
              "content": ""
            },
            {
              "type": "added",
              "content": "| Rule defined in | `/path` resolves to |"
            },
            {
              "type": "added",
              "content": "| :- | :- |"
            },
            {
              "type": "added",
              "content": "| Project or local settings, such as `.claude/settings.json` | `<project root>/path` |"
            },
            {
              "type": "added",
              "content": "| User settings at `~/.claude/settings.json` | `~/.claude/path` |"
            },
            {
              "type": "added",
              "content": "| A file passed with `--settings <file>` | `<directory of file>/path` |"
            },
            {
              "type": "added",
              "content": "| CLI flags, `/permissions`, or session rules | `<original cwd>/path` |"
            },
            {
              "type": "added",
              "content": ""
            },
            {
              "type": "added",
              "content": "A deny rule such as `Read(/secrets/**)` in user settings blocks `~/.claude/secrets/**`, not a `secrets` directory in your project. To write a rule in user settings that applies inside every project, use a `//` absolute path or a `~/` home-relative path instead."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "On Windows, paths are normalized to POSIX form before matching. `C:\\Users\\alice` becomes `/c/Users/alice`, so use `//c/**/.env` to match `.env` files anywhere on that drive. To match across all drives, use `//**/.env`."
            },
            {
              "type": "context",
              "content": ""
            }
          ]
        }
      ]
    }
  ]
}
