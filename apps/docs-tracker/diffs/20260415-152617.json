{
  "id": "20260415-152617",
  "timestamp": "2026-04-15T15:26:17.875Z",
  "aiSummary": "tmux環境で24bitのトゥルーカラー出力を有効にするための新しい環境変数が追加されました。ネットワーク設定では、Unixソケット接続に関する制限事項がOSごとに詳しく明文化されています。特にLinuxやWSL2環境におけるseccompフィルタの影響と、それに対する設定の挙動が整理されました。開発環境のターミナル表示とセキュリティ設定の両面で、より具体的な仕様が示されています。",
  "files": [
    {
      "filename": "env-vars.md",
      "additions": 1,
      "deletions": 0,
      "explanation": "tmux内で24bitカラー表示を許可するための環境変数 CLAUDE_CODE_TMUX_TRUECOLOR に関する説明が追加されました。",
      "hunks": [
        {
          "header": "@@ -138,6 +138,7 @@ Claude Code supports the following environment variables to control its behavior",
          "lines": [
            {
              "type": "context",
              "content": "| `CLAUDE_CODE_TASK_LIST_ID` | Share a task list across sessions. Set the same ID in multiple Claude Code instances to coordinate on a shared task list. See [Task list](/en/interactive-mode#task-list) |"
            },
            {
              "type": "context",
              "content": "| `CLAUDE_CODE_TEAM_NAME` | Name of the agent team this teammate belongs to. Set automatically on [agent team](/en/agent-teams) members |"
            },
            {
              "type": "context",
              "content": "| `CLAUDE_CODE_TMPDIR` | Override the temp directory used for internal temp files. Claude Code appends `/claude-{uid}/` (Unix) or `/claude/` (Windows) to this path. Default: `/tmp` on macOS, `os.tmpdir()` on Linux/Windows |"
            },
            {
              "type": "added",
              "content": "| `CLAUDE_CODE_TMUX_TRUECOLOR` | Set to `1` to allow 24-bit truecolor output inside tmux. By default, Claude Code clamps to 256 colors when `$TMUX` is set because tmux does not pass through truecolor escape sequences unless configured to. Set this after adding `set -ga terminal-overrides ',*:Tc'` to your `~/.tmux.conf`. See [Terminal configuration](/en/terminal-config) for other tmux settings |"
            },
            {
              "type": "context",
              "content": "| `CLAUDE_CODE_USE_BEDROCK` | Use [Bedrock](/en/amazon-bedrock) |"
            },
            {
              "type": "context",
              "content": "| `CLAUDE_CODE_USE_FOUNDRY` | Use [Microsoft Foundry](/en/microsoft-foundry) |"
            },
            {
              "type": "context",
              "content": "| `CLAUDE_CODE_USE_MANTLE` | Use the Bedrock [Mantle endpoint](/en/amazon-bedrock#use-the-mantle-endpoint) |"
            }
          ]
        }
      ]
    },
    {
      "filename": "settings.md",
      "additions": 2,
      "deletions": 2,
      "explanation": "Unixソケットの許可設定について、LinuxやWSL2ではseccompの制約により allowAllUnixSockets の使用が必須である旨が追記されました。",
      "hunks": [
        {
          "header": "@@ -281,8 +281,8 @@ Configure advanced sandboxing behavior. Sandboxing isolates bash commands from y",
          "lines": [
            {
              "type": "context",
              "content": "| `filesystem.denyRead` | Paths where sandboxed commands cannot read. Arrays are merged across all settings scopes. Also merged with paths from `Read(...)` deny permission rules. | `[\"~/.aws/credentials\"]` |"
            },
            {
              "type": "context",
              "content": "| `filesystem.allowRead` | Paths to re-allow reading within `denyRead` regions. Takes precedence over `denyRead`. Arrays are merged across all settings scopes. Use this to create workspace-only read access patterns. | `[\".\"]` |"
            },
            {
              "type": "context",
              "content": "| `filesystem.allowManagedReadPathsOnly` | (Managed settings only) Only `filesystem.allowRead` paths from managed settings are respected. `denyRead` still merges from all sources. Default: false | `true` |"
            },
            {
              "type": "removed",
              "content": "| `network.allowUnixSockets` | Unix socket paths accessible in sandbox (for SSH agents, etc.) | `[\"~/.ssh/agent-socket\"]` |"
            },
            {
              "type": "removed",
              "content": "| `network.allowAllUnixSockets` | Allow all Unix socket connections in sandbox. Default: false | `true` |"
            },
            {
              "type": "added",
              "content": "| `network.allowUnixSockets` | (macOS only) Unix socket paths accessible in sandbox. Ignored on Linux and WSL2, where the seccomp filter cannot inspect socket paths; use `allowAllUnixSockets` instead. | `[\"~/.ssh/agent-socket\"]` |"
            },
            {
              "type": "added",
              "content": "| `network.allowAllUnixSockets` | Allow all Unix socket connections in sandbox. On Linux and WSL2 this is the only way to permit Unix sockets, since it skips the seccomp filter that otherwise blocks `socket(AF_UNIX, ...)` calls. Default: false | `true` |"
            },
            {
              "type": "context",
              "content": "| `network.allowLocalBinding` | Allow binding to localhost ports (macOS only). Default: false | `true` |"
            },
            {
              "type": "context",
              "content": "| `network.allowMachLookup` | Additional XPC/Mach service names the sandbox may look up (macOS only). Supports a single trailing `*` for prefix matching. Needed for tools that communicate via XPC such as the iOS Simulator or Playwright. | `[\"com.apple.coresimulator.*\"]` |"
            },
            {
              "type": "context",
              "content": "| `network.allowedDomains` | Array of domains to allow for outbound network traffic. Supports wildcards (e.g., `*.example.com`). | `[\"github.com\", \"*.npmjs.org\"]` |"
            }
          ]
        }
      ]
    }
  ]
}
