{
  "id": "20260613-212808",
  "timestamp": "2026-06-13T21:28:08.236Z",
  "aiSummary": "利用可能なモデルを制限する availableModels 設定の挙動が強化され、新たに enforceAvailableModels オプションが導入されました。管理設定やポリシー設定でこれらを使用することで、ユーザーのサブスクリプションに基づくデフォルトモデルの解決を制限リスト内に強制できます。また、v2.1.175 以降では、管理設定による利用可能モデルの指定がユーザー設定とマージされず、完全に置き換わるように仕様変更されました。起動時やモデル切り替え時に、制限されたモデルが指定された場合の警告やフォールバック処理についても詳細が明文化されています。",
  "files": [
    {
      "filename": "model-config.md",
      "additions": 15,
      "deletions": 7,
      "explanation": "モデル選択の制限をデフォルトモデルにも適用する enforceAvailableModels 設定の詳細と、管理設定が他の設定を上書きする際のマージ挙動の変更が追加されました。",
      "hunks": [
        {
          "header": "@@ -115,7 +115,7 @@ When `availableModels` is set, the allowlist applies to every surface where a us",
          "lines": [
            {
              "type": "context",
              "content": "- **Advisor model**: the configured [`advisorModel`](/en/advisor) setting"
            },
            {
              "type": "context",
              "content": "- **Fallback chains**: elements of a [fallback model chain](#fallback-model-chains) outside the list are dropped"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "removed",
              "content": "Switching to a blocked model with `/model` is rejected with an error, while a blocked `--model` flag or `ANTHROPIC_MODEL` value is ignored at startup and the session starts on the default model. A blocked subagent or advisor override falls back to the inherited or default model rather than failing the request."
            },
            {
              "type": "added",
              "content": "Switching to a blocked model with `/model` is rejected with an error, while a blocked `--model` flag or `ANTHROPIC_MODEL` value is replaced at startup with a warning naming both the requested and substituted models, and the session starts on the default model. A blocked subagent or advisor override falls back to the inherited or default model rather than failing the request."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "```json"
            },
            {
              "type": "context",
              "content": "{"
            }
          ]
        },
        {
          "header": "@@ -125,37 +125,43 @@ Switching to a blocked model with `/model` is rejected with an error, while a bl",
          "lines": [
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "### Default model behavior"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "removed",
              "content": "The Default option in the model picker is not affected by `availableModels`. It always remains available and represents the system's runtime default [based on the user's subscription tier](#default-model-setting)."
            },
            {
              "type": "added",
              "content": "By default, the Default option in the model picker is not affected by `availableModels`. It remains available and represents the system's runtime default [based on the user's subscription tier](#default-model-setting)."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "removed",
              "content": "Even with `availableModels: []`, users can still use Claude Code with the Default model for their tier."
            },
            {
              "type": "added",
              "content": "To extend the allowlist to the Default option, set `enforceAvailableModels` to `true` in managed or policy settings alongside a non-empty `availableModels` list. When the tier default is not in the allowlist, Default resolves to the first allowed entry instead of the tier default. This requires Claude Code v2.1.175 or later."
            },
            {
              "type": "added",
              "content": ""
            },
            {
              "type": "added",
              "content": "An empty `availableModels` array never engages enforcement. Even with `availableModels: []`, users can still use Claude Code with the Default model for their tier regardless of `enforceAvailableModels`."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "### Control the model users run on"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "The `model` setting is an initial selection, not enforcement. It sets which model is active when a session starts, but users can still open `/model` and pick Default, which resolves to the system default for their tier regardless of what `model` is set to."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "removed",
              "content": "To fully control the model experience, combine three settings:"
            },
            {
              "type": "added",
              "content": "To fully control the model experience, combine these settings:"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "- **`availableModels`**: restricts which named models users can switch to"
            },
            {
              "type": "added",
              "content": "- **`enforceAvailableModels`**: extends the `availableModels` allowlist to the Default option, so Default cannot resolve to a model outside the list"
            },
            {
              "type": "context",
              "content": "- **`model`**: sets the initial model selection when a session starts"
            },
            {
              "type": "context",
              "content": "- **`ANTHROPIC_DEFAULT_SONNET_MODEL`** / **`ANTHROPIC_DEFAULT_OPUS_MODEL`** / **`ANTHROPIC_DEFAULT_HAIKU_MODEL`** / **`ANTHROPIC_DEFAULT_FABLE_MODEL`**: control what the Default option and the `sonnet`, `opus`, `haiku`, and `fable` aliases resolve to"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "removed",
              "content": "This example starts users on Sonnet 4.5, limits the picker to Sonnet and Haiku, and pins Default to resolve to Sonnet 4.5 rather than the latest release:"
            },
            {
              "type": "added",
              "content": "This example starts users on Sonnet 4.5, limits the picker to Sonnet and Haiku, and ensures Default resolves to a model on the allowlist rather than the tier default:"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "```json"
            },
            {
              "type": "context",
              "content": "{"
            },
            {
              "type": "context",
              "content": "  \"model\": \"claude-sonnet-4-5\","
            },
            {
              "type": "context",
              "content": "  \"availableModels\": [\"claude-sonnet-4-5\", \"haiku\"],"
            },
            {
              "type": "added",
              "content": "  \"enforceAvailableModels\": true,"
            },
            {
              "type": "context",
              "content": "  \"env\": {"
            },
            {
              "type": "context",
              "content": "    \"ANTHROPIC_DEFAULT_SONNET_MODEL\": \"claude-sonnet-4-5\""
            },
            {
              "type": "context",
              "content": "  }"
            },
            {
              "type": "context",
              "content": "}"
            },
            {
              "type": "context",
              "content": "```"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "removed",
              "content": "Without the `env` block, a user who selects Default in the picker would get the latest Sonnet release, bypassing the version pin in `model` and `availableModels`."
            },
            {
              "type": "added",
              "content": "Without `enforceAvailableModels` or the `env` block, a user who selects Default in the picker would get the latest release for their tier, bypassing the version pin in `model` and `availableModels`. The two settings cover different scopes: `enforceAvailableModels` makes Default obey the allowlist, while the `env` block pins which version a permitted alias such as `sonnet` resolves to. Use `enforceAvailableModels` alone when restricting model families is enough; add the `env` block when you also need to pin a specific version."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "### Merge behavior"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "removed",
              "content": "When `availableModels` is set at multiple levels, such as user settings and project settings, arrays are merged and deduplicated. To enforce a strict allowlist, set `availableModels` in managed or policy settings which take highest priority."
            },
            {
              "type": "added",
              "content": "When `availableModels` is set in user, project, and local settings only, arrays are merged and deduplicated across those levels."
            },
            {
              "type": "added",
              "content": ""
            },
            {
              "type": "added",
              "content": "When `availableModels` is set in managed or policy settings, the managed or policy value replaces the merged result entirely: entries added in user or project settings cannot widen it. Managed and policy settings replace lower-precedence values for `enforceAvailableModels` the same way. As of Claude Code v2.1.175, this is the only way to enforce a strict allowlist; earlier versions merge the managed list with lower-precedence entries."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "### Mantle model IDs"
            },
            {
              "type": "context",
              "content": ""
            }
          ]
        },
        {
          "header": "@@ -190,6 +196,8 @@ and Sonnet's efficiency for execution.",
          "lines": [
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "The plan-mode Opus phase uses the same context window as the `opus` model setting. On subscription tiers where Opus is [automatically upgraded to 1M context](#extended-context), `opusplan` receives the upgrade in plan mode as well. To force 1M context for both phases when you are not on an auto-upgrade tier, set the model to `opusplan[1m]`."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "added",
              "content": "When [`availableModels`](#restrict-model-selection) excludes Opus, `opusplan` stays on Sonnet in plan mode instead of switching. The same applies to the implicit Haiku-to-Sonnet plan-mode upgrade when Sonnet is excluded."
            },
            {
              "type": "added",
              "content": ""
            },
            {
              "type": "context",
              "content": "For a hybrid approach where Claude decides mid-task when to consult a second model rather than switching at the plan boundary, see the [advisor tool](/en/advisor)."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "### Fallback model chains"
            }
          ]
        }
      ]
    },
    {
      "filename": "settings.md",
      "additions": 6,
      "deletions": 3,
      "explanation": "設定項目一覧に enforceAvailableModels が追加され、availableModels が例外的にマージではなく上書き（リプレース）対象となることが明記されました。",
      "hunks": [
        {
          "header": "@@ -168,6 +168,8 @@ Security-enforcement fields are handled per field instead of being stripped whol",
          "lines": [
            {
              "type": "context",
              "content": "| :- | :- |"
            },
            {
              "type": "context",
              "content": "| `allowedMcpServers` | Enforced as an empty allowlist, so no MCP servers are admitted until the value is fixed. An individual invalid entry is stripped and the valid subset is enforced. |"
            },
            {
              "type": "context",
              "content": "| `allowManagedMcpServersOnly` | Treated as `true`. |"
            },
            {
              "type": "added",
              "content": "| `availableModels` | Enforced as an empty allowlist, so only the Default model is available until the value is fixed. An individual non-string entry is stripped and the valid subset is enforced. Applies in v2.1.175 and later. |"
            },
            {
              "type": "added",
              "content": "| `enforceAvailableModels` | Treated as `true`. Applies in v2.1.175 and later. |"
            },
            {
              "type": "context",
              "content": "| `forceLoginOrgUUID` | No organization is permitted to log in until the value is fixed. |"
            },
            {
              "type": "context",
              "content": "| `deniedMcpServers` | An individual invalid entry is stripped and the valid subset is enforced. A wholly invalid value is dropped with a warning, since denying every server would block servers the policy never named. |"
            },
            {
              "type": "context",
              "content": ""
            }
          ]
        },
        {
          "header": "@@ -206,7 +208,7 @@ This tolerance applies only to managed settings. User, project, and local settin",
          "lines": [
            {
              "type": "context",
              "content": "| `autoMode` | Customize what the [auto mode](/en/permission-modes#eliminate-prompts-with-auto-mode) classifier blocks and allows. Contains `environment`, `allow`, `soft_deny`, and `hard_deny` arrays of prose rules. Include the literal string `\"$defaults\"` in an array to inherit the built-in rules at that position. See [Configure auto mode](/en/auto-mode-config). Not read from shared project settings | `{\"soft_deny\": [\"$defaults\", \"Never run terraform apply\"]}` |"
            },
            {
              "type": "context",
              "content": "| `autoScrollEnabled` | In [fullscreen rendering](/en/fullscreen), follow new output to the bottom of the conversation. Default: `true`. Appears in `/config` as **Auto-scroll**. Permission prompts still scroll into view when this is off | `false` |"
            },
            {
              "type": "context",
              "content": "| `autoUpdatesChannel` | Release channel to follow for updates. Use `\"stable\"` for a version that is typically about one week old and skips versions with major regressions, or `\"latest\"` (default) for the most recent release. To disable auto-updates entirely, set [`DISABLE_AUTOUPDATER`](/en/setup#disable-auto-updates) in `env` | `\"stable\"` |"
            },
            {
              "type": "removed",
              "content": "| `availableModels` | Restrict which models users can select for the main session, [subagents](/en/sub-agents), and the [advisor](/en/advisor). Does not affect the Default option. See [Restrict model selection](/en/model-config#restrict-model-selection) | `[\"sonnet\", \"haiku\"]` |"
            },
            {
              "type": "added",
              "content": "| `availableModels` | Restrict which models users can select for the main session, [subagents](/en/sub-agents), and the [advisor](/en/advisor). See [Restrict model selection](/en/model-config#restrict-model-selection) | `[\"sonnet\", \"haiku\"]` |"
            },
            {
              "type": "context",
              "content": "| `awaySummaryEnabled` | Show a one-line session recap when you return to the terminal after a few minutes away. Set to `false` or turn off Session recap in `/config` to disable. Same as [`CLAUDE_CODE_ENABLE_AWAY_SUMMARY`](/en/env-vars) | `true` |"
            },
            {
              "type": "context",
              "content": "| `awsAuthRefresh` | Custom script that modifies the `.aws` directory (see [advanced credential configuration](/en/amazon-bedrock#advanced-credential-configuration)) | `aws sso login --profile myprofile` |"
            },
            {
              "type": "context",
              "content": "| `awsCredentialExport` | Custom script that outputs JSON with AWS credentials (see [advanced credential configuration](/en/amazon-bedrock#advanced-credential-configuration)) | `/bin/generate_aws_grant.sh` |"
            }
          ]
        },
        {
          "header": "@@ -231,6 +233,7 @@ This tolerance applies only to managed settings. User, project, and local settin",
          "lines": [
            {
              "type": "context",
              "content": "| `effortLevel` | Persist the [effort level](/en/model-config#adjust-effort-level) across sessions. Accepts `\"low\"`, `\"medium\"`, `\"high\"`, or `\"xhigh\"`. Written automatically when you run `/effort` with one of those values. `--effort` and [`CLAUDE_CODE_EFFORT_LEVEL`](/en/env-vars) override this for one session. See [Adjust effort level](/en/model-config#adjust-effort-level) for supported models | `\"xhigh\"` |"
            },
            {
              "type": "context",
              "content": "| `enableAllProjectMcpServers` | Automatically approve all MCP servers defined in project `.mcp.json` files | `true` |"
            },
            {
              "type": "context",
              "content": "| `enabledMcpjsonServers` | List of specific MCP servers from `.mcp.json` files to approve | `[\"memory\", \"github\"]` |"
            },
            {
              "type": "added",
              "content": "| `enforceAvailableModels` | When `true` and `availableModels` is a non-empty list in managed or policy settings, the Default model is also constrained to the allowlist. See [Restrict model selection](/en/model-config#restrict-model-selection) for details and the [merge behavior](/en/model-config#merge-behavior) when `availableModels` is set at multiple levels. Requires Claude Code v2.1.175 or later | `true` |"
            },
            {
              "type": "context",
              "content": "| `env` | Environment variables applied to every session and to subprocesses Claude Code spawns from it. As of v2.1.143, `NO_COLOR` and `FORCE_COLOR` set here are passed to subprocesses but do not change Claude Code's own interface colors. Set those in your shell before launching `claude` to change interface colors | `{\"FOO\": \"bar\"}` |"
            },
            {
              "type": "context",
              "content": "| `fallbackModel` | Fallback model(s) to try in order when the primary model is overloaded or unavailable. Claude Code switches to the next available model in the chain for the rest of the turn and shows a notice. `\"default\"` expands to the default model. Chains are capped at three models; extra entries are ignored. Unlike most array settings, this key does not merge across settings files: the highest-precedence file that defines it supplies the entire chain. The [`--fallback-model`](/en/cli-reference#cli-flags) flag overrides this for one session. See [Fallback model chains](/en/model-config#fallback-model-chains) | `[\"claude-sonnet-4-6\", \"claude-haiku-4-5\"]` |"
            },
            {
              "type": "context",
              "content": "| `fastModePerSessionOptIn` | When `true`, fast mode does not persist across sessions. Each session starts with fast mode off, requiring users to enable it with `/fast`. The user's fast mode preference is still saved. See [Require per-session opt-in](/en/fast-mode#require-per-session-opt-in) | `true` |"
            }
          ]
        },
        {
          "header": "@@ -570,7 +573,7 @@ This hierarchy ensures that organizational policies are always enforced while st",
          "lines": [
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "For example, if your user settings set `permissions.defaultMode` to `acceptEdits` and a project's shared settings set it to `default`, the project value applies. The example below covers how array-valued settings such as permission rules combine instead."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "removed",
              "content": "**Array settings merge across scopes.** When the same array-valued setting (such as `sandbox.filesystem.allowWrite` or `permissions.allow`) appears in multiple scopes, the arrays are **concatenated and deduplicated**, not replaced. This means lower-priority scopes can add entries without overriding those set by higher-priority scopes, and vice versa. For example, if managed settings set `allowWrite` to `[\"/opt/company-tools\"]` and a user adds `[\"~/.kube\"]`, both paths are included in the final configuration. The one exception is [`fallbackModel`](#available-settings), an ordered chain where position carries meaning: the highest-precedence file that defines it supplies the entire value."
            },
            {
              "type": "added",
              "content": "**Array settings merge across scopes.** When the same array-valued setting (such as `sandbox.filesystem.allowWrite` or `permissions.allow`) appears in multiple scopes, the arrays are **concatenated and deduplicated**, not replaced. This means lower-priority scopes can add entries without overriding those set by higher-priority scopes, and vice versa. For example, if managed settings set `allowWrite` to `[\"/opt/company-tools\"]` and a user adds `[\"~/.kube\"]`, both paths are included in the final configuration. Two exceptions: [`fallbackModel`](#available-settings), an ordered chain where position carries meaning so the highest-precedence file that defines it supplies the entire value, and as of v2.1.175, [`availableModels`](#available-settings), where a managed or policy value replaces lower-precedence entries entirely. See [Merge behavior](/en/model-config#merge-behavior)."
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "### Verify active settings"
            },
            {
              "type": "context",
              "content": ""
            }
          ]
        },
        {
          "header": "@@ -592,7 +595,7 @@ The line confirms which files are being read, not which layer supplied each indi",
          "lines": [
            {
              "type": "context",
              "content": "- **Skills**: Custom prompts that can be invoked with `/skill-name` or loaded by Claude automatically"
            },
            {
              "type": "context",
              "content": "- **MCP servers**: Extend Claude Code with additional tools and integrations"
            },
            {
              "type": "context",
              "content": "- **Precedence**: Higher-level configurations (Managed) override lower-level ones (User/Project)"
            },
            {
              "type": "removed",
              "content": "- **Inheritance**: Settings merge across scopes; scalar values from higher-priority scopes override, and arrays concatenate (`fallbackModel` is the exception: the highest-precedence scope supplies the whole chain)"
            },
            {
              "type": "added",
              "content": "- **Inheritance**: Settings merge across scopes; scalar values from higher-priority scopes override, and arrays concatenate. Exceptions: `fallbackModel`, where the highest-precedence scope supplies the whole chain, and `availableModels`, where a managed or policy value replaces lower-precedence entries"
            },
            {
              "type": "context",
              "content": ""
            },
            {
              "type": "context",
              "content": "### System prompt"
            },
            {
              "type": "context",
              "content": ""
            }
          ]
        }
      ]
    }
  ]
}
